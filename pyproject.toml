[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pynop"
version = "0.1.13"
description = "Shared DB/core + NopCommerce tooling for ABC StoreDataSync"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "ABC Sync", email = "noreply@example.com" }]
dependencies = []

[tool.setuptools]
packages = ["pynop", "pynop.core"]

